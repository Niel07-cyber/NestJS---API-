meta {
  name: POST /login
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "writer_user",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
